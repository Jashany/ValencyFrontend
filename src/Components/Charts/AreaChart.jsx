import React from "react";
import {
  AreaChart,
  Area,
  XAxis,
  YAxis,
  CartesianAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
} from "recharts";

const AreaChart = ({ prop }) => {
  return (
  <></>
)};

export default AreaChart;
